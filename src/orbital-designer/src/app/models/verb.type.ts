/**
 * Enum representation of different type of http verb
 */
export enum VerbType {
  GET = 0,
  PUT = 1,
  DELETE = 2,
  POST = 3,
  HEAD = 4,
  TRACE = 5,
  PATCH = 6,
  CONNECT = 7,
  OPTIONS = 8,
  CUSTOM = 9,
  NONE = 255,
}
