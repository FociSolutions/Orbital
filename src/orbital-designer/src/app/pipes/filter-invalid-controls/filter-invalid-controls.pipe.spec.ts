import { FilterInvalidControlsPipe } from './filter-invalid-controls.pipe';

describe('FilterInvalidControlsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterInvalidControlsPipe();
    expect(pipe).toBeTruthy();
  });
});
